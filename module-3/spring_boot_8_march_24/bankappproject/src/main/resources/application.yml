server:
  port: 8090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cbre?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
